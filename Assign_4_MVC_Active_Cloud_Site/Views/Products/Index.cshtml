@model IEnumerable<Assign_4_MVC_Active_Cloud_Site.Models.Products>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name_brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.industry_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.industry_code)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name_brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.industry_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.industry_code)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
